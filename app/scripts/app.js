define([
    'angular',
    'angular-resource',
    'angular-route',
    'jquery'
],
function (angular) {
    var app = angular.module('angDepControlsApp', ['ngResource', 'ngRoute']);
    return app;
});